# Создание проекта 
## Команды в теминале OS
* ```npm -v``` - проверить версию nmp
* ```node -v``` - проверить версию node
* ```npm create vite {название проекта}``` - запустить создание проекта
> после закпуска будет несколько вопросов
1. Select a framework: -> ● React
1. Select a variant: -> JavaScript
1. Install with npm and start now? -> Yes  

ждем, выведит это
>  VITE v8.0.8  ready in 362 ms  
>
>  ➜  Local:   http://localhost:5173/  
>  ➜  Network: use --host to expose  
>  ➜  press h + enter to show help    

и открываем в Vs Code через команду code .
## Команды в теминале VS code 
все эти команды пишем уже в терминале vs code   
```npm install``` - установка npm в проект  
```npm run dev``` -запуск проекта  (даст нам порт) 

# Основы React
## Создание карты поиска
В папке ```src``` создаем файл ```MovieCard.jsx```  
В этом файлк пишем:
```jsx
export function MovieCard() {
  return <div>MovieCard</div>
}
```
добавим входящие данные в эту функцию:
```jsx
export function MovieCard({image, rating}) {
  return <div>MovieCard</div>
}
```

Добавим поле картинки и рейтинга во фрагмент
```jsx
export function MovieCard({image, rating}) {
  return <div>
    <div>
      IMDb rating^ {rating}
    </div>
    <img src={image} alt="" />
  </div>
}
```

Далее:  
1. Удалим файл ```src/App.css``` 
1. изменим файл ```src/App.tsx```